# Core HTTP server and client
aiohttp>=3.8.0
yarl>=1.6.0

# HTML scraping
beautifulsoup4>=4.9.0

# DICOM Modality Worklist server (optional — MWL SCP disabled if not installed)
pynetdicom>=2.0.0
pydicom>=2.3.0

# Structured LLM extraction (schema validation, grammar-constrained generation
# against an external OpenAI-compatible server — see llm.cfg)
pydantic>=2
